apiVersion: v1
kind: Service
metadata:
  name: {{ include "md2odf.fullname" . }}
  labels:
    {{- include "md2odf.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      appProtocol: http
  selector:
    {{- include "md2odf.selectorLabels" . | nindent 4 }}
