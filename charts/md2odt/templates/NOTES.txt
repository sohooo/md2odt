md2odt is available inside the cluster at:

  http://{{ include "md2odt.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

{{- if .Values.httpRoute.enabled }}

The HTTPRoute is configured for:
{{- range .Values.httpRoute.hostnames }}
  http://{{ . }}{{ $.Values.httpRoute.pathPrefix }}
{{- end }}
{{- else }}

Enable Gateway API routing with:

  --set httpRoute.enabled=true \
  --set httpRoute.parentRefs[0].name=<gateway-name> \
  --set httpRoute.parentRefs[0].namespace=<gateway-namespace> \
  --set httpRoute.hostnames[0]=md2odt.example.com
{{- end }}
