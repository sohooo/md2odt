apiVersion: v1
kind: Service
metadata:
  name: {{ include "md2odt.fullname" . }}
  labels:
    {{- include "md2odt.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      appProtocol: http
  selector:
    {{- include "md2odt.selectorLabels" . | nindent 4 }}
